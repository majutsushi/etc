# handle deleted and then recreated directories
_dir_cycle() {
    if ! [[ . -ef $PWD ]]; then
        if ! cd -- "${PWD}" >/dev/null 2>&1; then
            echo "Warning: ${PWD} does no longer exist"
            return 1
        fi
    fi
}
add-zsh-hook precmd _dir_cycle

if [[ -f $HOME/.local/etc/zshrc ]]; then
    source $HOME/.local/etc/zshrc
fi

# vim: filetype=zsh
